# Copy this file to .env and fill in your key. .env is gitignored; nothing
# reads the key from anywhere else -- no literal default, no CLI argument.

DEEPSEEK_API_KEY=

# Optional: override the API base URL (defaults to https://api.deepseek.com).
# DEEPSEEK_BASE_URL=
